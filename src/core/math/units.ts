export const meters = (value: number) => value;
export const seconds = (value: number) => value;
export const kilograms = (value: number) => value;
export const newtons = (value: number) => value;

export const inchesToMeters = (inches: number) => inches * 0.0254;
export const feetToMeters = (feet: number) => feet * 0.3048;
